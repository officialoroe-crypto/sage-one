name: sage_one
description: SAGE ONE mobile command center.
publish_to: 'none'
version: 0.1.0+3

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0
  cupertino_icons: ^1.0.8
  google_sign_in: ^7.1.1
  google_sign_in_web: ^1.1.3
  flutter_secure_storage: ^9.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
